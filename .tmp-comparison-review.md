# Comparative security review — Paladin (custodian) vs iron-proxy vs NVIDIA OpenShell

Read this brief. Do NOT edit anything. Return a single consolidated comparison.

## The three codebases (all present in this worktree)

1. OURS — custodian-kernel 0.5.0 (pre-1.0, signed off this week by Claude + Codex).
   Key dirs:
   - paladin/ — encrypted credential broker: vault.py+crypto.py (keyfile-encrypted vault),
     broker.py (scoped grants/revocation), egress.py/egress_client.py (policy-filtered
     egress), sandbox.py (bwrap confinement, DEFAULT_MASKS for ~/.ssh, ~/.kube,
     ~/.docker, container-socket null-masking), git_credential.py (paladin:// URLs),
     audit.py + receipts.py (tamper-evident records), backup.py, bitwarden.py, dashboard.py
   - custodian/guards/ — harness guards (claude, codex, hermes): hook-level per-tool-call
     governance, approvals, receipts. custodian/guards/gate.py — dormant-by-default
     enablement gate (single state file, flock, fsync, symlink-safe).
   - custodian/packs/base.py — oracle gate: deterministic ClaimStatus verification
     (VERIFIED/CONTRADICTED/UNVERIFIABLE/PENDING) against ground truth, not a second LLM.
   - custodian/tools/registry.py — L0-L4 authority bands, cost caps, kernel decide() gate.
   ~3,000 tests. bwrap sandboxing with --unshare-user, per-file policy binds.

2. iron-proxy — ironsh/iron-proxy (Apache-2.0, 552 stars, Go, released Apr 2026, active).
   MITM egress proxy + built-in DNS server. Default-deny egress at the network
   boundary; upstream-IP deny list (SSRF/DNS-rebinding, IMDS blocked); boundary-level
   secret injection ("proxy tokens" swapped for real credentials at egress);
   per-request structured audit; PostgreSQL MITM with SET ROLE; signed releases.
   Location: .tmp-iron-proxy/ (cmd/, internal/, examples/, iron-proxy.example.yaml).

3. NVIDIA OpenShell — safe/private runtime for autonomous AI agents (Rust, Apache-2.0,
   8k stars, alpha per own README, extremely active). Sandboxed execution environments
   with declarative YAML policies: unauthorized file access, data exfiltration,
   uncontrolled network. Docker/Podman/MicroVM sandboxes, gateway architecture, Helm.
   Location: .tmp-openshell/ (crates/, docs/, deploy/, README.md — 429 .rs files).

## The task

Compare all three as agent-governance/egress-protection tooling. THE MOST IMPORTANT
INSTRUCTION: find weaknesses in ALL THREE, OURS INCLUDED. Do not cheerlead ours. If
Paladin/custodian loses to the others on a dimension, say so plainly. Ground every
claim in the actual code — skim strategically (each side has hundreds of files; focus
on the core threat-model surfaces: sandbox/isolation, secret handling, egress policy,
audit, and the decision/approval path).

## Output format (return as your final message)

1. ONE paragraph: what each tool actually is, in one line each.
2. A table: dimensions (isolation mechanism, secret handling, egress control,
   decision/approval layer, audit, deployment model, maturity) × three tools.
3. WEAKNESSES — bullet lists, severity-tagged (CRITICAL/HIGH/MEDIUM/LOW), for each
   tool separately, ours included. Look for: escape vectors, secret-leak paths,
   policy-bypass, TOCTOU/races, trust-boundary confusion, deployment foot-guns,
   missing pieces.
4. Where Paladin/custodian genuinely LOSES to one of the others (be specific and
   code-grounded).
5. Where Paladin/custodian genuinely WINS (code-grounded).
6. A one-line bottom line: which single tool, if any, you would pick for an
   agent-running setup, and why.
