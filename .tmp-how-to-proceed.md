# How-to-proceed review request — custodian-kernel 0.5.0 launch

Read the files referenced below. Do NOT edit anything. Return a recommendation.

## Verified, launch-safe state (release branch feat/confined-bubblewrap-beta, tip 4d8529a)
- Full test suite: 3002 passed, 0 failures
- Release manifest: all 9 prepare checks PASS (fresh install, PyPI upgrade,
  managed two-slot install, PEP 668, smoke), content_digest f63d63a5
- GitHub Actions CI on the public mirror (KeyArgo/custodian-kernel): 6/6
  green — Linux/Windows/macOS x Python 3.11/3.12, each running the installer
  e2e lifecycle (install -> launcher -> reinstall -> data-preserving uninstall)
- Installer hardened (16/16 tests), sandbox socket masking + watchdog in the wheel

## Pending decision (the review subject)
An UNCOMMITTED patch to scripts/build-kernel-release-tree.py (read it) adds
.github/workflows/ci.yml, ci/e2e_installer.py, tests/test_oss_installer.py and
docs/CONTAINMENT.md to the generated public release tree, so the cross-platform
CI keeps working in the public mirror AFTER publish (the mirror is regenerated
from this tree; without the change, .github/ would be wiped at publish and CI
would silently vanish).

Status of that patch:
- The patch tool's internal syntax lint passed, but NOTHING has executed it.
- It is currently UNRUNNABLE: the worktree lacks .github/ and ci/ (the copy
  command to create them from the mirror was denied by the operator).
- The operator denied the verification/fix commands (tree build, py_compile,
  copies). Repeatedly. So the release pipeline right now would FAIL the tree
  build if run.
- The committed state at 4d8529a is fully verified and launch-safe WITHOUT
  the patch.

## The options
A. Complete the zero-problem steps: copy .github/ + ci/ into the worktree,
   run the tree build, re-prep (9 checks), commit, remove stale
   release-manifests/kernel-0.5.0-beta1/ + alpha1/ dirs.
B. Roll back the builder patch; ship the verified 4d8529a state as-is
   (accept that CI may vanish from the public repo at publish).
C. Something else (say what).

## Questions
1. Which option, and why, given the launch is imminent?
2. Am I missing any risk in the publish flow (mirror regeneration, CI
   persistence, manifest staleness, the live guard hook that treats
   release-manifests/ paths as production and prompts for human approval)?
3. Is the patch itself correct (read it) — any bug in the copytree/copy2
   additions, ordering, or paths?
4. What is the minimal safe set of steps for whichever option you recommend,
   in order, with verification for each step?
